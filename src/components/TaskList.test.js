/* import { render, screen } from "@testing-library/react";
import TaskList from "./TaskList";

describe("Given a TaskList component", () => {
  describe("When it recives a task list", () => {
    test("Then it should render this tasks", () => {});
    const toDos = [
      {
        task: "Go to the shop",
        isDone: false,
        id: 1,
      },
      {
        task: "Walk with the dog",
        isDone: false,
        id: 2,
      },
    ];

    render(<TaskList />);

    const task1 = screen.getByText("Go to the shop");

    expect(task1).toBeInTheDocument();
  });
});
 */
